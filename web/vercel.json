{
  "framework": "vite",
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "env": {
    "VITE_API_URL": {
      "description": "URL of the deployed Sofa Syndicate API (e.g. https://sofa-syndicate-api.onrender.com)"
    }
  }
}
